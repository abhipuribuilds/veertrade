FROM python:3.13-slim
WORKDIR /app
COPY services/api/pyproject.toml .
RUN pip install --no-cache-dir .[dev] uvicorn[standard]
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
